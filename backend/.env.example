# Example backend env file (do NOT commit real secrets)
# Copy this to `.env` locally or set variables in your host's environment (Render, systemd, etc.)

# Django secret
SECRET_KEY=replace-with-a-secure-secret

# Frontend URL used in emails (password reset links)
FRONTEND_URL=http://localhost:3000

# Email/SMPP settings
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@example.com
EMAIL_HOST_PASSWORD=your-email-password-or-app-password

# Optional: port to run Django on
PORT=8000
